server:
  port: 8580

spring:
  application:
    name: utils-support-runtime-agent-demo

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
