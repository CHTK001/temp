# utils-support-remote-ssh-agent-starter

SSH Agent — SSH 反向隧道 + 命令执行

---

## 快速开始

### 1. 添加依赖

```xml
<dependency>
    <groupId>com.chua</groupId>
    <artifactId>utils-support-remote-ssh-agent-starter</artifactId>
    <version>${project.version}</version>
</dependency>
```

---

## 功能概览

| 类/接口 | 说明 |
|---------|------|
| `SshAgentService` | SshAgentService |

---

## 配置说明

本模块为零配置模块，引入依赖后即可使用。

---

## 依赖关系

```
utils-support-remote-ssh-agent-starter
├── utils-support-common-starter
├── utils-support-remote-agent-starter
├── utils-support-ssh-starter
```