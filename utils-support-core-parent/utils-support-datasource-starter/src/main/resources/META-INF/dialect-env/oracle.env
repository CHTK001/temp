# Oracle 方言默认配置
#
quote-open="
quote-close="
driver=oracle.jdbc.OracleDriver
#
type.BIGINT=NUMBER(19)
type.BINARY=RAW(2000)
type.BIT=NUMBER(1)
type.BLOB=BLOB
type.BOOLEAN=NUMBER(1)
type.CHAR=CHAR(255)
type.CLOB=CLOB
type.DATE=DATE
type.DECIMAL=NUMBER(10,0)
type.DOUBLE=FLOAT
type.FLOAT=BINARY_FLOAT
type.INTEGER=NUMBER(10)
type.LONGNVARCHAR=NCLOB
type.LONGVARCHAR=CLOB
type.NUMERIC=NUMBER(10,0)
type.REAL=BINARY_FLOAT
type.SMALLINT=NUMBER(5)
type.TIME=TIMESTAMP
type.TIMESTAMP=TIMESTAMP
type.TINYINT=NUMBER(3)
type.VARBINARY=RAW(2000)
type.VARCHAR=VARCHAR2(255)
#
auto-increment-keyword=GENERATED BY DEFAULT AS IDENTITY
alter-column-string=MODIFY
current-timestamp-sql=SELECT SYSDATE FROM DUAL
#
# Oracle 不支持内联 COMMENT，使用独立 COMMENT ON 语句
table-comment=
column-comment=
#
# Oracle UPSERT（MERGE INTO）
upsert-template=merge into {table} using dual on ({whereClause}) when matched then update set {updateSet} when not matched then insert ({columns}) values ({values})
