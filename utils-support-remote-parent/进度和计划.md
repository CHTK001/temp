# 远控网关：进度与计划

> 更新时间：2026-09-07 · 版本 4.0.0.42 · 仓库 `utils-support-parent-starter/utils-support-remote-parent`

## 一、总体架构

```
utils-support-remote-parent
├── utils-support-remote-protocol     // 信令/帧/能力模型/SPI（AgentType 三模式、Session.agentType、NegotiatedCodec.targetEncoding）
├── utils-support-remote-core         // 零拷贝传输（FrameServer/FrameClient/FrameCodec 二进制帧、RemoteTransport/RemoteFlow/RemoteServer/Client）
├── utils-support-remote-gateway      // 网关核心：信令 + 转码（GatewayServer、AuthManager、SessionManager、RouteManager、TranscodeEngine+GatewayTranscoder）
├── utils-support-remote-agent        // 被控端（三模式：FORWARD 纯转发 / SHELL 套壳自启 freerdp 或自研 / PUSH 纯自研实时推送仅监控）
└── utils-support-remote-controller   // 控制端（仅 Web + Java 套 Web 壳；ControllerClient、DecoderRenderer、InputInjector）
```

**控制端架构约束**（用户确认）：
- 控制端只有两种形态：**Web 浏览器** 和 **Java 套 Web 壳**（内嵌 Web 服务器）
- 控制端协议：
  - **SSH 走 WebSocket**：SSH 命令通过 WebSocket 传输到 agent 执行
  - **远程桌面走 WebRTC**：RDP 视频流通过 WebRTC 传输
- 控制端输入：账号密码（自研的不需要显示）、agentID（含 ip、端口）、验证码、ip、端口等
- SSH 支持开启反向隧道（agent 在 NAT 后时）

**Agent 三模式**（用户确认）：
| 模式 | 能力来源 | 说明 |
|---|---|---|
| FORWARD | 三方软件（freerdp/vnc/ssh） | 被控端仅做会话桥接转发 |
| SHELL | 自启 freerdp+转发，或自研 | Java 启动器套壳 |
| PUSH | 纯自研 | 实时画面推送，**仅监控无远程控制**（控制端已实现禁控守卫） |

**编解码协商规则**（用户确认，TranscodeEngine 已实现）：
1. 被控端编码优先：按**被控端**编码列表顺序找与控制端解码能力的交集（如默认 `h264` 优先、`jpeg` 兜底）
2. 交集为空（都不一致）→ **网关转码**：源=被控端第一个编码，目标=控制端第一个支持编码（`transcoded=true`，`GatewayTranscoder.transcode` 兜底执行）
3. 服务端不额外推 JPEG/不做无关转码——一致时帧原样透传

**单向连通场景**（用户确认：agent 无法被网关主动连接）：
- 典型 NAT 场景：agent 能出连网关，网关不能入连 agent
- 解决方案：控制端参数通知网关是否启用反向隧道
- 控制端在会话建立时通知网关 `reverseTunnelEnabled=true`
- 网关启用后，agent 主动创建 **SSH 反向隧道**到网关（`SshClient.TunnelDefinition.remote()`）
- 网关通过反向隧道的本地端口反向访问 agent
- agent 注册时 `AgentInfo.extra` 上报 `reverseTunnelPort`/`gatewayLocalPort`
- 网关读取该信息后通过本地端口连接 agent

**网关核心职责**（用户确认）：
- **信令**：agent/controller 注册、鉴权（验证码/令牌）、会话管理、消息路由、编解码协商
- **转码**：编解码能力交集为空时，网关执行 h264↔jpeg 转码
- **连接策略**：一个请求一个连接，不复用连接，提高吞吐
- 网关不关心具体业务逻辑，只负责信令转发和编解码适配

## 整体流程图

### 场景一：正常双向连通（三端）

```
┌──────────────┐  WebSocket(信令+SSH)  ┌──────────────┐
│  控制端       │ ◄───────────────────── │   网关       │
│  (Web/套Web壳)│                        │              │
│              │  WebSocket(媒体/RDP)    │              │
│              │ ◄───────────────────── │              │
└──────────────┘                        │  AuthManager │
                                        │ SessionManager│
                                        │ RouteManager │
                                        │ TranscodeEngine│
                                        │ (一请求一连接) │
                                        └──────┬───────┘
                                               │ 正向隧道
                                               │ WebSocket
                                               ▼
┌──────────────┐                        ┌──────────────┐
│  被控端(Agent)│ ◄───────────────────── │  目标机器     │
│              │    正向隧道            │  (RDP/VNC)   │
│  AgentService│    (agent→目标)        │              │
│  SSHTunnelMgr│                        │              │
└──────────────┘                        └──────────────┘

被控端模式：SERVICE(自研) / SHELL(套壳freerdp/vnc/ssh) / PUSH(仅监控)
```

### 场景二：单向连通 + 反向隧道（三端）

```
控制端参数通知网关开启反向隧道 → reverseTunnelEnabled=true

┌──────────────┐  WebSocket(信令)     ┌──────────────┐
│  控制端       │ ──reverseTunnel──▶   │   网关       │
│  (Web/套Web壳)│  enabled=true       │              │
│              │ ◄───────────────────── │              │
└──────────────┘                        │  AuthManager │
                                        │ SessionManager│
                                        │ RouteManager │
                                        │ (一请求一连接) │
                                        └──────┬───────┘
                                               │ SSH 反向隧道
                                               │ agent→网关
                                               │ gateway:2223
                                               ▼
┌──────────────┐                        ┌──────────────┐
│  被控端(Agent)│ ◄───────────────────── │  网关本地端口  │
│  (NAT后)    │    反向隧道             │  (2223)      │
│              │    agent→gateway       │              │
│  SSHTunnelMgr│                        │              │
│  createReverse│                       │              │
│  Tunnel()    │                        │              │
└──────────────┘                        └──────────────┘

AgentInfo.extra: {reverseTunnelPort=2223, gatewayLocalPort=2223}
```

### 反向隧道时序（控制端参数驱动）

```
控制端                              网关                              被控端(Agent)
┌─────────┐                        ┌─────────┐                       ┌─────────┐
│          │──会话请求─────────────▶│          │                       │          │
│          │  reverseTunnelEnabled  │          │                       │          │
│          │  =true                  │          │                       │          │
│          │                        │          │──注册信息─────────────▶│          │
│          │                        │          │  (含reverseTunnelPort) │          │
│          │                        │          │                       │          │
│          │◀─注册应答───────────── │          │                       │          │
│          │                        │          │                       │          │
│          │──反向隧道指令──────────▶│          │                       │          │
│          │  enableReverseTunnel   │          │──创建反向隧道─────────▶│          │
│          │                        │          │  SshClient.TunnelDefinition│
│          │                        │          │  .remote(port,...)     │          │
│          │                        │          │                       │          │
│          │                        │◀─反向─────│                       │          │
│          │                        │  gateway:2223                     │          │
│          │                        │                       │          │
│          │──通过反向隧道───────────│──反向隧道──────────▶│          │
│          │  执行命令/控制         │  gateway:2223      │          │
│          │                        │                       │          │
└─────────┘                        └─────────┘                       └─────────┘
```

**单端连通处理流程**：
1. 控制端连接网关，会话参数通知 `reverseTunnelEnabled=true`
2. Agent 启动 → 连接网关（WebSocket 信令通道）
3. Agent 创建 SSH 反向隧道到网关（`SSHTunnelManager.createReverseTunnel`）
4. Agent 注册到网关，`AgentInfo.extra` 包含 `reverseTunnelPort`
5. 网关读取 `reverseTunnelPort` → 通过本地端口连接 agent
6. 控制端通过网关 → 反向隧道 → agent 执行命令
7. **网关策略**：一请求一连接，不复用连接，提高吞吐

## 二、当前进度（已完成并验证）

### 1. 零拷贝传输层（remote-core）✅
- 二进制定长前缀帧：`totalLen|type|sidLen|metaLen|payloadLen|sessionId|meta(json)|payload`，**无 buffered 流、无 Base64、无字符串中转**
- 写路径 gather（payload `ByteBuffer.wrap` 零复制）；读路径 payload 直读目标 byte[]
- `FrameServer`/`FrameClient`：NIO 阻塞通道 + 虚拟线程一连接一线程；hello 握手（首帧 CTRL+meta{kind:hello, clientId}）注册连接身份，`connect()` 同步等 ack
- `RemoteTransport`/`RemoteFlow` API不变换底座；连接标识=定向路由键（控制端传接入令牌）
- 修复：readBinary 跳长度字段误用 1 字节 → 字段错位、读线程静默死亡（虚拟线程任务须兜底 catch Exception）
- 修复：`FrameServer.closeQuietly(SocketChannel)` 缺失 → 添加静态 helper；`FrameCodec.writeBinary` 参数从 `WritableByteChannel` 改为 `GatheringByteChannel`（`SocketChannel` 实现此接口，支持 `write(ByteBuffer[])` gather 写）

### 2. 控制端 Java 侧（remote-controller）✅
- `ControllerClient`：以接入令牌注册连接、信令上报 ControllerInfo、startSession（预生成 sessionId + Session 信令携带 verifyCode 元数据）、closeSession、sessionEstablished、PUSH 会话拒绝键鼠注入
- `DecoderRenderer`：热路径仅解码一次（1080p 162ms→33ms/帧），`setFrameListener` 交付嵌入方绘制；缩略图由解码结果缩放
- 冒烟 `RemoteCoreControllerSmokeTest` **11/11**（含 256KB 大帧字节级一致）

### 3. 网关与协议 ✅
- 鉴权模型：agentRegister 首次注册登记验证码（重复注册校验一致性）；controllerConnect 令牌即凭据首次接入注册；createSession 校验被控端验证码
- handleSignal 按 kind 元数据分发 AgentInfo/ControllerInfo 注册信令；CTRL 帧按会话→agentId 定向路由
- Session 携带 agentType；NegotiatedCodec 含 encoding/targetEncoding/width/height/quality/transcoded

### 4. 性能基准（RemotePerfBenchmark）
- 编解码：二进制 1015~2577 MB/s vs JSON+Base64 66~79 MB/s（**16~40x**）；线上体积省 ~36%
- 真实 TCP 全双工（300 帧/档全达）：720p 上行 205 MB/s；**1080p 下行 6328fps/396MB/s、上行 6764fps/423MB/s**
- 渲染热路径去 4.9x 浪费；下一个天花板：1080p 解码 33ms（ImageIO）→ 计划换 javacv/ffmpeg

### 5. 全部 6 模块 reactor 编译通过 ✅
- remote-protocol、remote-core、remote-gateway、remote-agent、remote-controller 全部 **BUILD SUCCESS**
- 改动已入库（`git add` + commit）

### 6. 原生采集三平台（remote-agent）✅
- `NativeScreenCapture`：Windows GDI（`CreateCompatibleDC+BitBlt+GetDIBits`）/ **Linux X11**（`XOpenDisplay+XGetImage`——XImage 为 PointerType，`XImageStruct` 按 Xlib 布局 Structure 映射读取字段）/ **macOS CoreGraphics**（`CGDisplayCreateImage`——jna-platform 无 mac 封装，自定义 `CoreGraphics` JNA 直连）——三平台占位符清零
- `NativeFrame` 原始像素帧：采集/编码全程**不经 BufferedImage**（raw 字节显式拷贝）；多屏原始行拼接（`System.arraycopy`）
- 采集性能：逐像素 `setRGB` → raster `DataBufferInt` 直写（约 10x）；GetDIBits 经 `Memory` 显式读写（非零拷贝共享缓冲）
- `ScreenCapture` 包装 `NativeScreenCapture`：`captureFrame()` 返回 `NativeFrame`，`capture()`/`captureBufferedImage()` 仅消费边界转换

### 7. 自研编码 SPI 架构（remote-agent）✅
- `FrameEncoderSpi`（原始帧编码契约——`codecName()/encode(NativeFrame,quality)/close()`）
- `@Spi("jpeg")` `JpegFrameEncoder`：`RawJpegEncoder`（`PixelInterleavedSampleModel` 包装原始字节 + `RawRasterImage`（RenderedImage）→ ImageIO ImageWriter——无 BufferedImage）
- `@Spi("h264")` `H264FrameEncoder`：`NativeVideoCodec`（Rust JNI 原生）——RGB→BGR24 显式转换 → `h264Encode`（句柄生命周期 + 尺寸变更重建）
- `NativeEncoder` 按协商编码名经 `ServiceProvider.of(FrameEncoderSpi.class)` 加载（缓存 + close 传播）；默认编码集 `["h264","jpeg"]`（h264 优先）
- 修复：`RenderedImage.getSources()` JDK25 返回 `Vector`（非 List）；`Gdi32`→`GDI32`（jna-platform 5.12.1 全大写）；`Structure.fromPointer`→`newInstance`；`WinDef.HANDLE`→`WinNT.HANDLE`；`WinGDI.SRCCOPY` 缺失用字面量 `0x00CC0020`

### 8. 网关转码执行（remote-gateway）✅
- `GatewayTranscoder`：H264↔JPEG 双向——JPEG→H264（ImageIO 解码 → BGR24 → 原生 `h264Encode`）；H264→JPEG（原生 `h264DecoderCreate/h264Decode` → RGB → Raster 包装 JPEG——无 BufferedImage 中间态）
- `TranscodeEngine.negotiate` 修复：`needTranscode` 大小写 bug（`"jpeg"≠"JPEG"` 恒 true → 仅交集为空才转码）；空交集时源=被控端编码、目标=控制端解码能力（`NegotiatedCodec.targetEncoding`）
- `GatewayServer.routeData` 钩子：`transcoded=true` 会话的 DATA 帧先 `TranscodeEngine.transcode` 再 `publish`（控制端↔网关交互点的编解码适配）

### 9. 桌面支持上报与门控（remote-agent）✅
- `AgentInfo.desktopSupported` + `AgentBootstrap.detectDesktopSupported()`（Windows/macOS 恒真；Linux 查 `DISPLAY`——无则 headless）
- 桌面门控：PUSH（桌面采集）模式 + `desktopSupported=false` → **拒绝桌面连接、降级到套壳模式**（SSH/RDP/VNC 仍可用）——控制端连接协议：选 SSH 走 SSH；选桌面由 Linux headless 判定拒绝
- `AgentBootstrap.main` 部署入口：`args[0]=网关地址 / [1]=agentId / [2]=verifyCode / [3]=agentType(PUSH/SHELL)`

### 10. 套壳模式完整实现（remote-agent）✅
- **`AgentInfo` 新增字段**：`host`/`port`/`username`/`password`（套壳模式目标机配置）+ `gatewaySshHost`/`gatewaySshPort`/`gatewaySshUser`/`gatewaySshPass`（网关 SSH 配置，用于反向隧道），`serialVersionUID` 不变
- **`SSHTunnelManager` 重写**：
  - 正向隧道：`createRDPTunnel`/`createVNCTunnel`/`createSSHTunnel`（agent→目标）
  - **反向隧道**：`createReverseTunnel`（agent→网关），解决 NAT/防火墙后网关无法主动连 agent 的问题
  - `createReverseTunnel(int gatewayLocalPort, String agentHost, int agentPort)`：通过 `SshClient.TunnelDefinition.remote()` 创建反向隧道
  - `startFreerdp`：通过 `127.0.0.1:rdpLocalPort` 连接
  - `startVncViewer`：通过 `127.0.0.1:vncLocalPort` 连接
  - `startSshShellViaClient`：用 `SshClient.terminal()` 启动 PTY 交互式终端
  - `closeAll`：先关反向隧道→再关正向隧道
  - 新增 `findAvailablePort()` 查找可用本地端口
- **`AgentShellService` 重写**：
  - `start()` 中先创建反向隧道（`createReverseTunnel`），再启动正向隧道会话
  - `createReverseTunnel()`：检查 `AgentInfo.gatewaySshHost`，通过 `SSHTunnelManager.createReverseTunnel` 建立反向隧道
  - 反向隧道端口上报到 `AgentInfo.extra`（`reverseTunnelPort`/`gatewayLocalPort`），供网关识别
  - `determineShellMode()`：根据 `platform` 判断 RDP/VNC/SSH
  - `startRDPSession()`：从 `AgentInfo` 读取 `host/port/username/password`，创建 SSH 隧道 + 启动 freerdp
  - `startVNCSession()`：同流程，创建 VNC 隧道 + 启动 vncviewer
  - `startSSHSession()`：创建 SSH 隧道 + 启动 PTY 终端
  - **`TelnetServer` 集成**：套壳模式启动时自动启动 4567 端口 Telnet Shell（APM 监控、状态查看）
  - `stop()` 调用 `telnetServer.stop()`
- **依赖**：`utils-support-runtime-shell`（TelnetServer/ShellSession/Telnet 命令系统）

### 11. 本轮修改记录（2026-09-07 会话处理）✅

**本轮处理内容**（全量实现套壳模式的 freerdp/ssh/vnc + 编译修复）：

1. **`AgentInfo.java`**：新增 `host`/`port`/`username`/`password` 四个字段（套壳模式目标机配置），`serialVersionUID` 不变（向后兼容）
2. **`SSHTunnelManager.java`**：完全重写
   - `createRDPTunnel`/`createVNCTunnel`/`createSSHTunnel`：先建立 SSH 隧道（`SshClient.builder` + `SshTunnel`），客户端再通过 `127.0.0.1:localPort` 连接
   - `startFreerdp`：通过 `127.0.0.1:rdpLocalPort` 连接（SSH 隧道转发后的本地端口）
   - `startVncViewer`：通过 `127.0.0.1:vncLocalPort` 连接
   - `startSshShellViaClient`：用 `SshClient.terminal()` 启动 PTY 交互式终端
   - `startSshShell`：通过 SSH 隧道启动 `ssh` 命令行
   - `closeAll`：按类型关闭所有隧道和客户端
   - 新增 `findAvailablePort()` 查找可用本地端口
3. **`AgentShellService.java`**：完全重写
   - `determineShellMode()`：根据 `platform` 判断 RDP/VNC/SSH
   - `startRDPSession()`：从 `AgentInfo` 读取 `host/port/username/password`，创建 SSH 隧道 + 启动 freerdp
   - `startVNCSession()`：同流程，创建 VNC 隧道 + 启动 vncviewer
   - `startSSHSession()`：创建 SSH 隧道 + 启动 PTY 终端
   - **`TelnetServer` 集成**：套壳模式启动时自动启动 4567 端口 Telnet Shell（APM 监控、状态查看）
   - `stop()` 调用 `telnetServer.stop()`
4. **`remote-agent/pom.xml`**：`jna`/`jna-platform` 版本从 5.14.0/5.12.1 冲突 → 统一为 5.12.1
5. **`FrameServer.java`**：新增 `closeQuietly(SocketChannel)` 静态 helper 方法，安全关闭通道
6. **`FrameCodec.java`**：`writeBinary` 参数类型从 `WritableByteChannel` 改为 `GatheringByteChannel`（支持 `write(ByteBuffer[])` gather 写）
7. **编译修复**：
   - `NativeScreenCapture.java`：`X11.AllPlanes`、`WinDef.HANDLE`→`WinNT.HANDLE`、`WinGDI.SRCCOPY` 缺失、`GetDIBits` 签名等 JNA API 问题
   - `RawRasterImage.java`：`RenderedImage.getSources()` JDK25 返回 `Vector`（非 `List`）
   - `remote-core`：`FrameServer.closeQuietly` 缺失 + `FrameCodec.writeBinary` 参数类型不匹配

## 三、控制端架构约束（用户确认）

- 控制端只有 **Web 浏览器** 和 **Java 套 Web 壳**（内嵌 Web 服务器）两种形态
- 控制端协议：
  - **SSH 走 WebSocket**：SSH 命令通过 WebSocket 传输（信令通道）
  - **远程桌面走 WebRTC**：RDP 视频流通过 WebRTC 传输（媒体通道）
- 控制端输入项：账号密码（自研不需要显示）、agentID（含 ip、端口）、验证码、ip、端口等
- SSH 支持开启反向隧道（agent 在 NAT 后时，agent 主动创建反向隧道到网关）
- **网关核心 = 信令 + 转码**：网关只做信令路由和编解码转码，不关心业务逻辑

## 四、计划（按优先级）

1. **Web 控制端（Java 套 Web）**：`WebControllerServer`（remote-controller）自研 HTTP+WS 桥接——
   - 透传被控端原始流到浏览器（不做转码，遵循协商结果）
   - 浏览器键鼠事件 JSON → CTRL 帧（复用 PUSH 禁控守卫）
   - 以接入令牌注册网关（与 Java 控制端一致）
   - 页面按载荷嗅探选渲染：JPEG(FFD8) → canvas 直绘；H264 → MSE/WebRTC 播放器钩子
   - 支持 SSH（WebSocket 通道）和 RDP（WebRTC 通道）两种协议
   - SSH 支持反向隧道配置
2. **WebRTC**：浏览器端原生 RTCPeerConnection + DataChannel；Java 侧栈选型 `dev.onvoid.webrtc:webrtc-java`（Maven Central 当前不可达，需私库或离线引入 natives）；信令端点按 WHEP 风格（POST offer→answer）；PUSH 模式帧 → VideoSource(I420) → H264/VP8
3. **网关 Web 控制端接入 + 反向隧道控制**：网关支持 Web 浏览器作为控制端通过 WebSocket 连接——控制器输入项：账号密码、agentID(含ip/端口)、验证码、ip、端口、**reverseTunnelEnabled（是否开启反向隧道）**
4. **网关单端连通处理**：当 agent 无法注册到网关（单向连通）时，agent 通过反向隧道上报 `reverseTunnelPort`，网关通过该端口反向连接 agent 完成注册
5. ~~**网关转码接入路由**~~：✅ 已实现（`TranscodeEngine.transcode` + `GatewayServer.routeData` 钩子——transcoded 会话的 DATA 帧先转码再转发）
6. **Agent 身份路由修复**：`AgentBootstrap` 建连改为 `new RemoteClient(agentInfo.getId(), gatewayUrl)`——FORWARD/SHELL 接收网关定向指令的前提（当前随机 clientId，注册信令不受影响）
7. **会话建立信令闭环**：网关 handleSignal 增加会话建立分发（校验 verifyCode → createSession → 向双方回执协商结果），打通控制端 startSession 与网关会话
8. **H264 渲染路径**：DecoderRenderer 当前仅 JPEG 解码（嗅探 FFD8）——H264 帧的解码渲染待接入（编码侧 `H264FrameEncoder` 已就绪；解码侧可复用 `NativeVideoCodec.h264DecoderCreate/h264Decode`）
9. **端到端联调**：真实 agent(PUSH) + 网关 + Web 控制端三端联调——**联调环境已提供**（agent 主机 Linux `124.221.230.112`；RDP 目标 Windows 测试服务器 `192.168.200.18`（RDP 3389 已确认可达，原生 RDP 协议））——部署轮进行中：需先重装 protocol（`targetEncoding` 字段后 m2 旧 jar）再打包 gateway（上次中断点）
10. ~~**AgentInfo 字段补充**~~：✅ 已实现（`host`/`port`/`username`/`password`/`gatewaySshHost`/`gatewaySshPort`/`gatewaySshUser`/`gatewaySshPass`）
11. ~~**SSHTunnelManager 重写**~~：✅ 已实现（正向隧道 + 反向隧道 `createReverseTunnel`）
12. ~~**TelnetServer 集成**~~：✅ 已实现（`AgentShellService` 启动 4567 端口 Shell 管理界面）
13. ~~**反向隧道（NAT 穿透）**~~：✅ 已实现（agent 主动创建反向 SSH 隧道到网关，网关通过 `gatewayLocalPort` 反向访问 agent；端口上报到 `AgentInfo.extra`）

## 五、协作约定（双会话）

- 本会话：remote-core + remote-controller；并行会话：remote-gateway + remote-agent
- 同一工作树并发编辑：Edit 前必须重读文件；提交由并行会话周期性 sweep（`chore: agent updates`）
- 构建约定：Git Bash 必须前台 `mvn.cmd`（后台 fd 报错）；离线 `-o`；仓库无 JUnit（m2 无），验证用 main() 冒烟 + cp.txt classpath
- 冒烟复跑：见 `.workbuddy/memory/2026-09-07.md`

## 六、遗留问题备忘

- `NettyHttpSyncServer`（publish 不通知 listener）、`NettyWebSocketSyncClient`（硬编码 ws://localhost:8080）、`TcpSyncServer`（32KB 行缓冲溢出天花板）——common-starter 遗留缺陷，remote 已绕开（自研帧引擎），暂不修
- `RemoteConnection`/`AgentShellService`/`SSHTunnelManager` 待随 FORWARD/SHELL 模式落地完善
- **反向隧道与网关集成**：agent 创建反向隧道后，`AgentInfo.extra` 中包含 `reverseTunnelPort`/`gatewayLocalPort`。网关需读取该信息并连接到本地端口以反向访问 agent——网关侧 `SessionManager`/`RouteManager` 需适配反向隧道端口（待实现）
- **Web 控制端**：控制端需改造为 Web + Java 套 Web 壳——当前 `ControllerClient` 是独立 Java 应用，需改为通过 HTTP+WS 桥接 Web 页面
- **WebRTC 媒体通道**：RDP 视频流需通过 WebRTC 传输——Java 侧栈选型 `dev.onvoid.webrtc:webrtc-java`（Maven Central 当前不可达，需私库或离线引入 natives）
- **SSH 通过 WebSocket**：SSH 命令需通过 WebSocket 传输而非当前自定义帧协议——需 `AgentShellService` 支持 WebSocket 通道
- **网关单端连通处理**：当 agent 无法注册到网关时，网关需通过反向隧道端口反向连接 agent——网关侧 `SessionManager` 需适配
- **运行时未实测**（编译级验证通过）：Linux X11 / macOS CoreGraphics 原生采集、H264 编码（Rust JNI）、网关 H264↔JPEG 转码——需真机（Linux 服务器已提供）
- **RDP 原始码流转码**：freerdp 套壳模式下 RDP 码流（RemoteFX 等）不经网关（web 显示的是 agent 采集重编码流）；未来直传 RDP 流时需 freerdp 解码器做 RDP→h264/jpeg 转码（当前原生库仅 h264/h265——另项）
- **桌面探测深度**：目前仅查 `DISPLAY` 环境变量（静态）；X 服务实际可用性（DISPLAY 设置但 X 已死）未做运行时探测——可加 `XOpenDisplay` 探测（`NativeScreenCapture` 已有该路径）
- **jna-platform 依赖**：`utils-support-remote-agent` pom.xml 中显式声明 `jna` 5.12.1 + `jna-platform` 5.12.1，与 transitive 依赖版本对齐，避免 `Gdi32`/`X11`/`WinDef` 等类加载失败
- **`FrameCodec.writeBinary` 方法签名**：参数为 `GatheringByteChannel`（非 `WritableByteChannel`），调用方 `FrameServer` 传入的 `SocketChannel` 实现此接口
- **`FrameServer.closeQuietly`**：新增静态 helper 方法安全关闭 `SocketChannel`，避免空指针和异常传播
